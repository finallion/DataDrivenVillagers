# Loom defaults to Fabric. Without this the neoForge() dependency configuration is never registered
# and evaluating neoforge/build.gradle fails outright. Fabric needs no counterpart.
loom.platform=neoforge
